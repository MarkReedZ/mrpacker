environment:
  CIBW_BUILD: cp3?-*
  CIBW_SKIP: cp34-*

build_script:
  - pip install cibuildwheel==0.10.0
  - cibuildwheel --output-dir wheelhouse

artifacts:
  - path: "wheelhouse\\*.whl"
    name: Wheels
